import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

// for testing components with vite
// npm run dev
// to test the component in a standalone environment
createApp(App).mount('#app')
